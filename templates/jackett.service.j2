[Unit]
Description=Jackett Daemon
After=network.target

[Service]
User={{ jackett_user }}
Type=simple
ExecStart=/usr/bin/mono --debug {{ jackett_directory }}JackettConsole.exe --NoRestart
Restart=always
RestartSec=2
TimeoutStopSec=5

[Install]
WantedBy=multi-user.target
